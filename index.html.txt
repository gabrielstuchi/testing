<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Uma Cartinha Para Você 🍮💛</title>

  <style>
    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      font-family: 'Arial', sans-serif;
      background: linear-gradient(to bottom, #FFF6E5, #FFFFFF);
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      color: #8B5E3C;
    }

    .card {
      background: #FFFFFF;
      width: 90%;
      max-width: 420px;
      border-radius: 22px;
      padding: 25px;
      text-align: center;
      box-shadow: 0 12px 30px rgba(0,0,0,0.12);
      border: 3px dashed #F6D365;
    }

    h1 {
      margin-top: 10px;
      font-size: 1.6em;
    }

    p {
      font-size: 1.05em;
      line-height: 1.5;
    }

    img {
      max-width: 100%;
      border-radius: 16px;
    }

    .image-box {
      margin: 15px 0;
    }

    button {
      margin: 10px 6px;
      padding: 12px 22px;
      border: none;
      border-radius: 30px;
      font-size: 1em;
      cursor: pointer;
      background: #F6D365;
      color: #8B5E3C;
      transition: 0.3s;
    }

    button:
